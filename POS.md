واجهة نقطة البيع (POS) — دليل سريع

ملف الواجهة:
- `pos.html` — صفحة واجهة نقطة البيع للمستخدم/العميل.
- `pos.js` — سكربت المعالجة: عربة المشتريات، إنهاء البيع، وتسجيل الحركات في `localStorage`.

تشغيل الواجهة محليًا:
- افتح الملف مباشرة (انقر مزدوج أو من PowerShell):

```powershell
Start-Process "C:\Users\Shathan\Desktop\store-organizer\pos.html"
```

أو شغّل مجلّد المشروع بسيرفر محلي ثم افتح:

```powershell
cd "C:\Users\Shathan\Desktop\store-organizer"
python -m http.server 8000
# ثم افتح http://localhost:8000/pos.html
```

كيف تعمل الواجهة باختصار:
- البحث عن المنتج: اكتب اسم أو SKU واضغط زر "أضف" لإضافته للعربة.
- العربة: يمكنك زيادة/نقص الكمية أو حذف صنف.
- إنهاء البيع: اضغط "إنهاء البيع"، أدخل اسم العميل (اختياري) وطريقة الدفع، سيُسجل الطلب ويُنقص المخزون.
- إيصال: بعد الإنهاء سيظهر إيصال يمكنك طباعته.

ملاحظات تقنية:
- الواجهة تستخدم نفس مفتاح التخزين `store-organizer-v1` في `localStorage` لذلك تتشارك البيانات مع واجهة الإدارة (`index.html`).
- عند إنهاء البيع، تُنشأ حركات من النوع `out` لكل صنف وتُحدَّث كميات المنتجات تلقائياً.
- إذا كانت الكمية المطلوبة أكبر من المتوفر سيظهر تنبيه ولن يكتمل الإجراء.

إضافات جديدة في POS:
- إمكانية تطبيق نسبة ضريبة (٪) على الفاتورة.
- إمكانية إدخال خصم (مبلغ ثابت) يُخصم من الفاتورة.
- إدخال `مبلغ الدفع` وحساب `الباقي` تلقائياً قبل إنهاء البيع.
- يتم تضمين الضريبة والخصم والمبلغ المدفوع والباقي في الإيصال.
التطوير المستقبلي:
- ربط POS بقاعدة مركزية مثل Supabase لمزامنة المبيعات بين أجهزة متعددة.
- إضافة خصم/ضريبة وحسابات دفع متعددة.
- طباعة إيصال أكثر تنسيقاً أو إرسال إيصال عبر البريد.
